## Mariadb-Docker

### Description
A mariaDb instance running with docker.

### Instructions

- Copy the `.env.sample` to `.env` and replace the values.

```bash
    cp .env.sample .env
```

- start the container.
```bash
    docker-compose up -d
```
